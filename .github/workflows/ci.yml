name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

concurrency:
  group: escrowmint-py-ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lint-test-build:
    runs-on: ubuntu-latest
    outputs:
      coverage: ${{ steps.coverage.outputs.value }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: .python-version

      - name: Set up uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --dev

      - name: Check README install instructions
        run: |
          ! rg -n "Until the first PyPI release|git\\+https://github.com/biprakanta/escrowmint-py" README.md
          rg -n "uv add escrowmint" README.md
          rg -n "pip install escrowmint" README.md

      - name: Run lint
        run: uv run ruff check

      - name: Run tests
        run: uv run pytest | tee pytest-output.txt

      - name: Extract coverage
        id: coverage
        run: |
          python - <<'PY'
          import os
          import pathlib
          import re

          text = pathlib.Path("pytest-output.txt").read_text()
          match = re.search(r"Total coverage: ([0-9]+(?:\.[0-9]+)?)%", text)
          if match is None:
              raise SystemExit("Could not find coverage in pytest output")

          value = float(match.group(1))
          with pathlib.Path(os.environ["GITHUB_OUTPUT"]).open("a", encoding="utf-8") as fh:
              fh.write(f"value={value:.2f}\n")
          PY

      - name: Build distributions
        run: uv build

  publish-coverage-badge:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: lint-test-build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Generate coverage badge data
        env:
          COVERAGE: ${{ needs.lint-test-build.outputs.coverage }}
        run: |
          python - <<'PY'
          import json
          import os
          import pathlib

          value = float(os.environ["COVERAGE"])
          if value >= 90:
              color = "brightgreen"
          elif value >= 80:
              color = "green"
          elif value >= 70:
              color = "yellow"
          elif value >= 60:
              color = "orange"
          else:
              color = "red"

          out_dir = pathlib.Path("badge-data")
          out_dir.mkdir(exist_ok=True)
          payload = {
              "schemaVersion": 1,
              "label": "coverage",
              "message": f"{value:.2f}%",
              "color": color,
          }
          (out_dir / "coverage.json").write_text(json.dumps(payload))
          PY

      - name: Publish coverage badge data
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ github.token }}
          publish_branch: badges
          publish_dir: ./badge-data
